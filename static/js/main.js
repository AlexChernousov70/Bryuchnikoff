document.addEventListener('DOMContentLoaded', function() {
    // Инициализация системы уведомлений
    initToastSystem();
    
    // Убрать тестовое уведомление для production
    // Все остальные модули подключаются через отдельные файлы
});